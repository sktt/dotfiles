#!/bin/sh

# note: update-alternatives --config dmenu to use xft

. $HOME/.dotfiles/xorg/solarized.ini

dmenu_run -i -nb $base03 -nf $base0 -sb $base02 -sf $orange -fn 'Inconsolata\-dz for Powerline:style=dz:pixelsize=12' "$@"
